# Legacy implementation

`V1.2_nosave.ahk` is retained only as historical reference.

Do not run it alongside VaultLoop. It uses obsolete shortcuts, creates the legacy
firewall rule `123456`, does not scope the rule to a verified GTA executable, and
does not include the current watchdog or recovery behavior.
